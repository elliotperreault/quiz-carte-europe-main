<%- include('../partials/header.ejs', {page_title: "app"}) %>


<body class="bg-primary bg-gradient min-vh-100 d-flex flex-column justify-content-between">
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1 fs-2">Les pays d'Europe</span>
        </div>
    </nav>

    <main class="container d-flex flex-grow-1 align-items-center justify-content-center">
        <div class="card shadow-lg p-4 w-100" style="max-width: 900px;">
            <h2 class="card-title text-center mb-4">Associe chaque pays à sa lettre</h2>
            <div class="table-responsive">
                <table id="countriesTable" class="table table-bordered table-hover align-middle text-center">
                    <thead class="table-light">
                        <tr>
                            <th>Pays d'Europe</th>
                            <th>Lettre</th>
                            <th>Pays d'Europe</th>
                            <th>Lettre</th>
                        </tr>
                    </thead>
                    <tbody class="editable">
                        <tr>
                            <td>Portugal</td>
                            <td contenteditable="true"></td>
                            <td>Islande</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Belgique</td>
                            <td contenteditable="true"></td>
                            <td>Allemagne</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Bulgarie</td>
                            <td contenteditable="true"></td>
                            <td>Royaume-Uni</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Suède</td>
                            <td contenteditable="true"></td>
                            <td>Lituanie</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Espagne</td>
                            <td contenteditable="true"></td>
                            <td>Grèce</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Italie</td>
                            <td contenteditable="true"></td>
                            <td>Finlande</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Ukraine</td>
                            <td contenteditable="true"></td>
                            <td>Russie</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Pays-Bas</td>
                            <td contenteditable="true"></td>
                            <td>République tchèque</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Serbie</td>
                            <td contenteditable="true"></td>
                            <td>Roumanie</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Danemark</td>
                            <td contenteditable="true"></td>
                            <td>Autriche</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>France</td>
                            <td contenteditable="true"></td>
                            <td>Croatie</td>
                            <td contenteditable="true"></td>
                        </tr>
                        <tr>
                            <td>Pologne</td>
                            <td contenteditable="true"></td>
                            <td>Hongrie</td>
                            <td contenteditable="true"></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="d-flex justify-content-between mt-4">
                <button id="viewMapButton" type="button" class="btn btn-outline-primary btn-lg me-2">Voir carte</button>
                <button id="submitButton" type="button" class="btn btn-success btn-lg">Soumettre</button>
            </div>
        </div>
    </main>

        <div id="mapModal" class="modal" tabindex="-1" style="display:none;align-items:center;justify-content:center;">
            <div class="modal-dialog modal-dialog-centered" style="max-width: 95vw;">
                <div class="modal-content bg-white p-0 border-0 shadow-lg rounded-4">
                    <div class="modal-header border-0 pb-0">
                        <h5 class="modal-title">Carte des pays d'Europe</h5>
                        <button type="button" class="btn-close" aria-label="Fermer" style="font-size:2rem;" id="closeMapModal"></button>
                    </div>
                    <div class="modal-body d-flex justify-content-center align-items-center p-0">
                        <img src="/images/europeCountriesMap.png" alt="Carte d'Europe" class="img-fluid rounded-3" style="max-height:80vh;max-width:100%;object-fit:contain;">
                    </div>
                </div>
            </div>
        </div>
</body>
</html>

  <script>

    // --- Get Modal Elements ---
    const mapModal = document.getElementById("mapModal");
    const closeModalButton = document.getElementById("closeMapModal");

    document.addEventListener("input", (e) => {
        if (e.target.matches(`td[contenteditable="true"]`)) {

            if (e.target.innerHTML.length == 4) { 
                return e.target.classList.remove("full")
            }
            if (!e.target.classList.contains("full")) {
                if (document.querySelectorAll("td.full").length >= 15) {
                    e.target.innerHTML = e.target.innerHTML.slice(0,-1) 
                    alert("Tu ne peux pas identifier plus de 15 pays")
                    return; // Stop further processing
                }
            }

            if (e.target.innerHTML.length > 1) {
                 e.target.innerHTML = e.target.innerHTML.slice(0,1); 
            }

          
             if (e.target.innerHTML.length === 1) {
                 e.target.classList.add("full");
             } else {
                 e.target.classList.remove("full"); 
             }
        }
    })

    document.addEventListener("click", (e) => {
        if (e.target.matches("#viewMapButton")) {
            console.log("Viewing map");
            // --- Show the modal ---
            mapModal.style.display = "flex"; 

        } else if (e.target.matches("#submitButton")) {
            if (document.querySelectorAll("td.full").length < 15) {
                return alert("Il te reste " + (15 - document.querySelectorAll("td.full").length) + " pays à identifier")
            }
            let params = new URLSearchParams(window.location.search)
            params.append("countries", JSON.stringify(extractTableDataToJson("countriesTable")))

            window.location.href = "/app/capitals/" + '?' + params.toString();
        }
    })

    
    function closeModal() {
        mapModal.style.display = "none";
    }

   
    closeModalButton.addEventListener("click", closeModal);

    
    mapModal.addEventListener("click", (e) => {
        if (e.target === mapModal) { 
            closeModal();
        }
    });

    
    document.addEventListener('keydown', (e) => {
        if (e.key === "Escape" && mapModal.style.display === "flex") {
            closeModal();
        }
    });


    function extractTableDataToJson(tableId) {
        const table = document.getElementById(tableId);
        if (!table) {
            console.error(`Table with ID "${tableId}" not found.`);
            return null;
        }

        const data = {};
        const rows = table.querySelectorAll('tbody tr');

        rows.forEach(row => {
            const cells = row.querySelectorAll('td');


            if (cells.length >= 4) {

                const country1 = cells[0].textContent.trim();
                // Get value, ensure it's max 1 character, default to empty string if nothing entered
                const value1 = cells[1].textContent.trim().slice(0, 1);

                const country2 = cells[2].textContent.trim();
                // Get value, ensure it's max 1 character, default to empty string if nothing entered
                const value2 = cells[3].textContent.trim().slice(0, 1);

                if (country1) {
                    // Only add if value1 is not empty
                    if (value1) data[country1.toLowerCase()] = value1;
                }
                if (country2) {
                     // Only add if value2 is not empty
                    if (value2) data[country2.toLowerCase()] = value2;
                }
            } else {
                console.warn("Skipping row with unexpected cell count:", row);
            }
        });

        return data;
    }
  </script>